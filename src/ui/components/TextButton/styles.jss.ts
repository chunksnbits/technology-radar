import { typoDefault } from '../Styles';

export const styles = {
  root: {
    ...typoDefault(),

    display: 'inline-flex',
    flexDirection: 'row',
    alignItems: 'center',
    border: 0,
    padding: 0,
    margin: 0,
    outline: 0,
    cursor: 'pointer',
    background: 'none',
    appearance: 'none',
  },
}
